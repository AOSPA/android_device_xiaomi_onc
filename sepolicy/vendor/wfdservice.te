# Allow Cast Settings to access WFD prop
get_prop(wfdservice, vendor_audio_prop)
get_prop(wfdservice, vendor_system_prop)
get_prop(wfdservice, wfd_debug_prop)

# Allow access to firmware files for HDCP session
r_dir_file(wfdservice, vendor_firmware_file)
